- name: "manage worker1 and 2"
  hosts: all
  become: true
  tasks:
  - name: "ensure Sunil user exists"
    user:
      name: sunil
      state: present
  - name: "ensure Sunil user exists"
    user:
      name: sunil
      state: present
      
